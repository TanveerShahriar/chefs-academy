import React from 'react';
import { Container } from 'react-bootstrap';
import './Q2.css'

const Q2 = () => {
    return (
        <Container className='ans p-3 mt-5 m-lg-5 rounded text-white'>
            <h3>Why are you using firebase? What other options do you have to implement authentication?</h3>
            <p className='text-start'>Semantic tags are some HTML tags which are generally used to increase the readability of the code. It also helps the developer to work easily. It also helps for search engine optimization. Some of the semantic tags are header, footer, article, nav etc.
            </p>
        </Container>
    );
};

export default Q2;